version: 2

models:
  - name: dim_clientes
    description: "esta é a dimensão de clientes"
    columns:
      - name: cliente_sk
        description: >
          "esta é a chave surrogate da tabel de clientes, 
          utilizamos as colunas customerid e businessentityid para esta chave "
        tests:
          - unique 
          - not null
          